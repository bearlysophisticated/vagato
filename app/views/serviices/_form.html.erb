<div class="narrow-60">

<%= simple_form_for(@serviice) do |s| %>
  <% if @serviice.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@serviice.errors.count, "error") %> prohibited this serviice from being saved:</h2>

      <ul>
      <% @serviice.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    <%= s.input :name, label: 'Megnevezés' %>
    <%= s.input :code, label: 'Kód' %>
    <%= s.button :submit, 'Mentés', class: 'btn-primary' %>

<% end %>

</div>