<%= render 'filter/filter', :base_url => '/rooms' %>

<div class="rooms row">
    <% @rooms.each do |room| %>
        <div class="col-xs-6 col-sm-4 col-md-4 col-lg-3">
          <%= render 'rooms/thumbnail', :room => room %>
        </div>
    <% end %>
</div>